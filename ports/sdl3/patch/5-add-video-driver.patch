diff --git a/src/video/SDL_sysvideo.h b/src/video/SDL_sysvideo.h
index 1fdc5bf..89594e9 100644
--- a/src/video/SDL_sysvideo.h
+++ b/src/video/SDL_sysvideo.h
@@ -533,6 +533,7 @@ extern VideoBootStrap PS2_bootstrap;
 extern VideoBootStrap PSP_bootstrap;
 extern VideoBootStrap VITA_bootstrap;
 extern VideoBootStrap RISCOS_bootstrap;
+extern VideoBootStrap STANIX_bootstrap;
 extern VideoBootStrap N3DS_bootstrap;
 extern VideoBootStrap NGAGE_bootstrap;
 extern VideoBootStrap RPI_bootstrap;
diff --git a/src/video/SDL_video.c b/src/video/SDL_video.c
index 48ed85a..6d65f0e 100644
--- a/src/video/SDL_video.c
+++ b/src/video/SDL_video.c
@@ -137,6 +137,9 @@ static VideoBootStrap *bootstrap[] = {
 #ifdef SDL_VIDEO_DRIVER_RISCOS
     &RISCOS_bootstrap,
 #endif
+#ifdef SDL_VIDEO_DRIVER_STANIX
+    &STANIX_bootstrap,
+#endif
 #ifdef SDL_VIDEO_DRIVER_RPI
     &RPI_bootstrap,
 #endif
diff --git a/src/video/stanix/SDL_stanixevents.c b/src/video/stanix/SDL_stanixevents.c
new file mode 100644
index 0000000..0999b38
--- /dev/null
+++ b/src/video/stanix/SDL_stanixevents.c
@@ -0,0 +1,201 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifdef SDL_VIDEO_DRIVER_STANIX
+
+#include "../../events/SDL_events_c.h"
+
+#include "SDL_stanixvideo.h"
+#include "SDL_stanixevents.h"
+#include "SDL_stanixwindow.h"
+#include <input.h>
+#include <twm.h>
+
+static SDL_Keycode translate_keycode(long stanix_key) {
+    switch (stanix_key) {
+    case INPUT_KEY_ESC:
+		return SDLK_ESCAPE;
+	case INPUT_KEY_TAB:
+		return SDLK_TAB;
+	case INPUT_KEY_BACKSPACE:
+		return SDLK_BACKSPACE;
+	case INPUT_KEY_ENTER:
+		return SDLK_RETURN;
+	case INPUT_KEY_DELETE:
+		return SDLK_DELETE;
+	case INPUT_KEY_INSERT:
+		return SDLK_INSERT;
+	case INPUT_KEY_HOME:
+		return SDLK_HOME;
+	case INPUT_KEY_END:
+		return SDLK_END;
+	case INPUT_KEY_ARROW_UP:
+		return SDLK_UP;
+	case INPUT_KEY_ARROW_DOWN:
+		return SDLK_DOWN;
+	case INPUT_KEY_ARROW_LEFT:
+		return SDLK_LEFT;
+	case INPUT_KEY_ARROW_RIGHT:
+		return SDLK_RIGHT;
+	case INPUT_KEY_PAGE_UP:
+		return SDLK_PAGEUP;
+	case INPUT_KEY_PAGE_DOWN:
+		return SDLK_PAGEDOWN;
+	case INPUT_KEY_LSHIFT:
+		return SDLK_LSHIFT;
+	case INPUT_KEY_RSHIFT:
+		return SDLK_RSHIFT;
+	case INPUT_KEY_LCTRL:
+		return SDLK_LCTRL;
+	case INPUT_KEY_RCTRL:
+		return SDLK_RCTRL;
+	case INPUT_KEY_LALT:
+		return SDLK_LALT;
+	case INPUT_KEY_RALT:
+	case INPUT_KEY_ALTGR:
+		return SDLK_RALT;
+	case INPUT_KEY_NUM_LOCK:
+		return SDLK_NUMLOCKCLEAR;
+	case INPUT_KEY_SCROLL_LOCK:
+		return SDLK_SCROLLLOCK;
+	case INPUT_KEY_CAPS_LOCK:
+		return SDLK_CAPSLOCK;
+	case INPUT_KEY_F1:
+		return SDLK_F1;
+	case INPUT_KEY_F2:
+		return SDLK_F2;
+	case INPUT_KEY_F3:
+		return SDLK_F3;
+	case INPUT_KEY_F4:
+		return SDLK_F4;
+	case INPUT_KEY_F5:
+		return SDLK_F5;
+	case INPUT_KEY_F6:
+		return SDLK_F6;
+	case INPUT_KEY_F7:
+		return SDLK_F7;
+	case INPUT_KEY_F8:
+		return SDLK_F8;
+	case INPUT_KEY_F9:
+		return SDLK_F9;
+	case INPUT_KEY_F10:
+		return SDLK_F10;
+	case INPUT_KEY_F11:
+		return SDLK_F11;
+	case INPUT_KEY_F12:
+		return SDLK_F12;
+	case INPUT_KEY_GUI:
+		return SDLK_LGUI;
+	case INPUT_KEY_VOLUME_UP:
+		return SDLK_VOLUMEUP;
+	case INPUT_KEY_VOLUME_DOWN:
+		return SDLK_VOLUMEDOWN;
+    default:
+        return stanix_key;
+    }
+}
+
+static uint8_t translate_mouse_button(long stanix_button) {
+    switch (stanix_button) {
+    case INPUT_KEY_MOUSE_LEFT:
+    default:
+        return SDL_BUTTON_LEFT;
+    case INPUT_KEY_MOUSE_MIDDLE:
+        return SDL_BUTTON_MIDDLE;
+    case INPUT_KEY_MOUSE_RIGHT:
+        return SDL_BUTTON_RIGHT;
+    }
+}
+
+static bool is_mouse_key(long key) {
+    if (key == INPUT_KEY_MOUSE_LEFT
+        || key == INPUT_KEY_MOUSE_MIDDLE
+        || key == INPUT_KEY_MOUSE_RIGHT) {
+        return true;
+    }
+    return false;
+}
+
+static SDL_Window *get_window(twm_window_t twm_window) {
+    int count;
+    SDL_Window **windows = SDL_GetWindows(&count);
+    for (int i=0; i<count; i++) {
+        SDL_Window *window = windows[i];
+        SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+        if (data->window == twm_window) {
+            return window;
+        }
+    }
+    return NULL;
+}
+
+static void handle_input_event(twm_event_input_t *event) {
+    switch (event->type) {
+    case TWM_INPUT_KEY:
+        if (is_mouse_key(event->key.key)) {
+            SDL_Mouse *mouse = SDL_GetMouse();
+            SDL_SendMouseButton(0, mouse->focus, SDL_DEFAULT_MOUSE_ID, translate_mouse_button(event->key.key), event->key.flags & TWM_INPUT_PRESS);
+        } else {
+            // TODO : focus the correct window
+            SDL_SendKeyboardKey(0, SDL_DEFAULT_KEYBOARD_ID, event->key.key, translate_keycode(event->key.key), event->key.flags & TWM_INPUT_PRESS);
+        }
+        break;
+    case TWM_INPUT_MOVE:
+        //SDL_SendMouseMotion(0, mouse->focus, SDL_DEFAULT_MOUSE_ID, false, (float)x, (float)y);
+        break;
+    }
+}
+
+void STANIX_PumpEvents(SDL_VideoDevice *_this)
+{
+    twm_event_t *event;
+    while ((event = twm_peek_event())) {
+        switch (event->type) {
+        case TWM_EVENT_INPUT:
+            handle_input_event((twm_event_input_t*)event);
+            break;
+        case TWM_EVENT_WINDOW_CLOSED:
+            twm_event_window_t *window_event = (twm_event_window_t*)event;
+            SDL_Window *window = get_window(window_event->window);
+            SDL_SendWindowEvent(window, SDL_EVENT_WINDOW_CLOSE_REQUESTED, 0, 0);
+            break;
+        case TWM_EVENT_WINDOW_FOCUS:;
+            window_event = (twm_event_window_t*)event;
+            window = get_window(window_event->window);
+            if (!window) break;
+            SDL_SendWindowEvent(window, SDL_EVENT_WINDOW_FOCUS_GAINED, 0, 0);
+            SDL_SetMouseFocus(window);
+            SDL_SetKeyboardFocus(window);
+            break;
+        case TWM_EVENT_WINDOW_UNFOCUS:
+            window_event = (twm_event_window_t*)event;
+            window = get_window(window_event->window);
+            SDL_SendWindowEvent(window, SDL_EVENT_WINDOW_FOCUS_LOST, 0, 0);
+            SDL_SetMouseFocus(NULL);
+            SDL_SetKeyboardFocus(NULL);
+            break;
+        }
+        free(event);
+    }
+}
+
+#endif // SDL_VIDEO_DRIVER_STANIX
diff --git a/src/video/stanix/SDL_stanixevents.h b/src/video/stanix/SDL_stanixevents.h
new file mode 100644
index 0000000..441ac5a
--- /dev/null
+++ b/src/video/stanix/SDL_stanixevents.h
@@ -0,0 +1,28 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifndef SDL_stanixevents_h_
+#define SDL_stanixevents_h_
+
+extern void STANIX_PumpEvents(SDL_VideoDevice *_this);
+
+#endif // SDL_stanixevents_h_
diff --git a/src/video/stanix/SDL_stanixframebuffer.c b/src/video/stanix/SDL_stanixframebuffer.c
new file mode 100644
index 0000000..6f42fee
--- /dev/null
+++ b/src/video/stanix/SDL_stanixframebuffer.c
@@ -0,0 +1,82 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifdef SDL_VIDEO_DRIVER_STANIX
+
+#include "../SDL_sysvideo.h"
+#include "SDL_stanixframebuffer.h"
+#include "SDL_stanixvideo.h"
+#include "SDL_stanixwindow.h"
+#include <unistd.h>
+#include <sys/mman.h>
+#include <twm.h>
+
+bool STANIX_CreateWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window, SDL_PixelFormat *format, void **pixels, int *pitch)
+{
+    SDL_WindowData *data = window->internal;
+    const SDL_DisplayMode *mode;
+
+    // destroy any already created framebuffer
+    STANIX_DestroyWindowFramebuffer(_this, window);
+
+    int fd;
+    if (twm_get_window_fb(data->window, &fd, &data->fb_info) < 0) {
+        SDL_SetError("Failed to fetch framebuffer");
+        return false;
+    }
+
+    data->framebuffer = mmap(NULL, data->fb_info.pitch * data->fb_info.height, PROT_WRITE, MAP_SHARED, fd, 0);
+    data->backbuffer  = SDL_malloc(data->fb_info.pitch * data->fb_info.height);
+    close(fd);
+
+    mode = SDL_GetCurrentDisplayMode(SDL_GetDisplayForWindow(window));
+    *format = mode->format;
+    *pixels = data->backbuffer;
+    *pitch = data->fb_info.pitch;
+
+    return true;
+}
+
+bool STANIX_UpdateWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window, const SDL_Rect *rects, int numrects)
+{
+    SDL_WindowData *data = window->internal;
+    // TODO : respect rects
+    (void)rects;
+    (void)numrects;
+    memcpy(data->framebuffer, data->backbuffer, data->fb_info.pitch * data->fb_info.height);
+    return twm_redraw_window(data->window, 0, 0, TWM_WHOLE_WIDTH, TWM_WHOLE_HEIGHT) >= 0;
+}
+
+void STANIX_DestroyWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window)
+{
+    SDL_WindowData *data = window->internal;
+    if (data->framebuffer) {
+        munmap(data->framebuffer, data->fb_info.pitch * data->fb_info.height);
+        data->framebuffer = NULL;
+    }
+    if (data->backbuffer) {
+        free(data->backbuffer);
+        data->backbuffer = NULL;
+    }
+}
+
+#endif // SDL_VIDEO_DRIVER_STANIX
diff --git a/src/video/stanix/SDL_stanixframebuffer.h b/src/video/stanix/SDL_stanixframebuffer.h
new file mode 100644
index 0000000..a157411
--- /dev/null
+++ b/src/video/stanix/SDL_stanixframebuffer.h
@@ -0,0 +1,31 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+
+#ifndef SDL_stanixframebuffer_h_
+#define SDL_stanixframebuffer_h_
+
+#include "SDL_internal.h"
+
+extern bool STANIX_CreateWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window, SDL_PixelFormat *format, void **pixels, int *pitch);
+extern bool STANIX_UpdateWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window, const SDL_Rect *rects, int numrects);
+extern void STANIX_DestroyWindowFramebuffer(SDL_VideoDevice *_this, SDL_Window *window);
+
+#endif // SDL_stanixframebuffer_h_
diff --git a/src/video/stanix/SDL_stanixmodes.c b/src/video/stanix/SDL_stanixmodes.c
new file mode 100644
index 0000000..f351e03
--- /dev/null
+++ b/src/video/stanix/SDL_stanixmodes.c
@@ -0,0 +1,59 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifdef SDL_VIDEO_DRIVER_STANIX
+
+#include "../SDL_sysvideo.h"
+#include "../../events/SDL_mouse_c.h"
+
+#include "SDL_stanixvideo.h"
+#include "SDL_stanixmodes.h"
+
+#include <twm.h>
+
+bool STANIX_InitModes(SDL_VideoDevice *_this)
+{
+    // TODO : remove this hack
+    SDL_DisplayMode mode = {
+		.format = SDL_PIXELFORMAT_XRGB8888,
+		.w = 1000,
+		.h = 800,
+		.refresh_rate = 60,
+	};
+
+    if (SDL_AddBasicVideoDisplay(&mode) == 0) {
+        return false;
+    }
+    return true;
+}
+
+bool STANIX_GetDisplayModes(SDL_VideoDevice *_this, SDL_VideoDisplay *display)
+{
+    return true;
+}
+
+bool STANIX_SetDisplayMode(SDL_VideoDevice *_this, SDL_VideoDisplay *display, SDL_DisplayMode *mode)
+{
+    return true;
+}
+
+#endif // SDL_VIDEO_DRIVER_STANIX
diff --git a/src/video/stanix/SDL_stanixmodes.h b/src/video/stanix/SDL_stanixmodes.h
new file mode 100644
index 0000000..b7aa06e
--- /dev/null
+++ b/src/video/stanix/SDL_stanixmodes.h
@@ -0,0 +1,30 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifndef SDL_stanixmodes_h_
+#define SDL_stanixmodes_h_
+
+extern bool STANIX_InitModes(SDL_VideoDevice *_this);
+extern bool STANIX_GetDisplayModes(SDL_VideoDevice *_this, SDL_VideoDisplay *display);
+extern bool STANIX_SetDisplayMode(SDL_VideoDevice *_this, SDL_VideoDisplay *display, SDL_DisplayMode *mode);
+
+#endif // SDL_stanixmodes_h_
diff --git a/src/video/stanix/SDL_stanixvideo.c b/src/video/stanix/SDL_stanixvideo.c
new file mode 100644
index 0000000..2824e20
--- /dev/null
+++ b/src/video/stanix/SDL_stanixvideo.c
@@ -0,0 +1,117 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifdef SDL_VIDEO_DRIVER_STANIX
+
+#include <unistd.h> // For getpid() and readlink()
+
+#include "../../core/linux/SDL_system_theme.h"
+#include "../../core/linux/SDL_progressbar.h"
+#include "../../events/SDL_keyboard_c.h"
+#include "../../events/SDL_mouse_c.h"
+#include "../SDL_pixels_c.h"
+#include "../SDL_sysvideo.h"
+
+#include "SDL_stanixvideo.h"
+#include "SDL_stanixwindow.h"
+#include "SDL_stanixmodes.h"
+#include "SDL_stanixevents.h"
+#include "SDL_stanixframebuffer.h"
+#include "twm.h"
+
+// Initialization/Query functions
+static bool STANIX_VideoInit(SDL_VideoDevice *_this);
+static void STANIX_VideoQuit(SDL_VideoDevice *_this);
+
+static void STANIX_DeleteDevice(SDL_VideoDevice *device)
+{
+    SDL_VideoData *data = device->internal;
+    SDL_free(data);
+    SDL_free(device);
+}
+
+static SDL_VideoDevice *STANIX_CreateDevice(void)
+{
+    SDL_VideoDevice *device;
+    SDL_VideoData *data;
+
+    // Initialize all variables that we clean on shutdown
+    device = (SDL_VideoDevice *)SDL_calloc(1, sizeof(SDL_VideoDevice));
+    if (!device) {
+        return NULL;
+    }
+    data = (struct SDL_VideoData *)SDL_calloc(1, sizeof(SDL_VideoData));
+    if (!data) {
+        SDL_free(device);
+        return NULL;
+    }
+    device->internal  = data;
+    device->VideoInit = STANIX_VideoInit;
+    device->VideoQuit = STANIX_VideoQuit;
+    device->free      = STANIX_DeleteDevice;
+
+    device->PumpEvents         = STANIX_PumpEvents;
+    device->CreateSDLWindow    = STANIX_CreateWindow;
+    device->DestroyWindow      = STANIX_DestroyWindow;
+    device->SetWindowTitle     = STANIX_SetWindowTitle;
+	device->SetWindowPosition  = STANIX_SetWindowPosition;
+	device->SetWindowSize      = STANIX_SetWindowSize;
+	device->SetWindowBordered  = STANIX_SetWindowBordered;
+	device->SetWindowResizable = STANIX_SetWindowResizable;
+    device->CreateWindowFramebuffer  = STANIX_CreateWindowFramebuffer;
+    device->UpdateWindowFramebuffer  = STANIX_UpdateWindowFramebuffer;
+    device->DestroyWindowFramebuffer = STANIX_DestroyWindowFramebuffer;
+
+    return device;
+}
+
+VideoBootStrap STANIX_bootstrap = {
+    "Stanix twm", "SDL Stanix twm video driver",
+    STANIX_CreateDevice,
+    NULL, // no message box
+    false
+};
+
+
+static bool STANIX_VideoInit(SDL_VideoDevice *_this)
+{
+    (void)_this;
+    if (twm_init(NULL) < 0) {
+        SDL_SetError("Failed to connect to the twm server");
+        SDL_LogDebug(SDL_LOG_CATEGORY_VIDEO, "Failed to connect to the twm server");
+        return false;
+    }
+    
+    if (!STANIX_InitModes(_this)) {
+        twm_fini();
+        return false;
+    }
+    return true;
+}
+
+void STANIX_VideoQuit(SDL_VideoDevice *_this)
+{
+    (void)_this;
+    twm_fini();
+}
+
+#endif // SDL_VIDEO_DRIVER_STANIX
diff --git a/src/video/stanix/SDL_stanixvideo.h b/src/video/stanix/SDL_stanixvideo.h
new file mode 100644
index 0000000..b033126
--- /dev/null
+++ b/src/video/stanix/SDL_stanixvideo.h
@@ -0,0 +1,40 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam  <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifndef SDL_stanixvideo_h_
+#define SDL_stanixvideo_h_
+
+#include "../SDL_sysvideo.h"
+#include "../../events/SDL_keymap_c.h"
+
+#include "../../core/linux/SDL_dbus.h"
+#include "../../core/linux/SDL_ime.h"
+#include <twm.h>
+
+// Private display data
+
+struct SDL_VideoData
+{
+    int stub;
+};
+
+#endif // SDL_x11video_h_
diff --git a/src/video/stanix/SDL_stanixwindow.c b/src/video/stanix/SDL_stanixwindow.c
new file mode 100644
index 0000000..2dd1030
--- /dev/null
+++ b/src/video/stanix/SDL_stanixwindow.c
@@ -0,0 +1,90 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifdef SDL_VIDEO_DRIVER_STANIX
+
+#include "../SDL_sysvideo.h"
+#include "../SDL_pixels_c.h"
+#include "../../events/SDL_keyboard_c.h"
+#include "../../events/SDL_mouse_c.h"
+#include "../../events/SDL_events_c.h"
+#include "../../core/unix/SDL_appid.h"
+
+#include "SDL_stanixvideo.h"
+#include "SDL_stanixwindow.h"
+#include <twm.h>
+
+extern bool STANIX_CreateWindow(SDL_VideoDevice *_this, SDL_Window *window, SDL_PropertiesID create_props)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)SDL_calloc(1, sizeof(SDL_WindowData));
+    data->window = twm_create_window("sdl window", window->w, window->h, TWM_NULL);
+    if (data->window == TWM_NULL) {
+        SDL_free(data);
+        return false;
+    }
+    window->internal = data;
+
+    // TODO : do various flags here
+    if (window->flags & SDL_WINDOW_BORDERLESS) {
+    }
+    return true;
+}
+
+extern void STANIX_DestroyWindow(SDL_VideoDevice *_this, SDL_Window *window)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    twm_destroy_window(data->window);
+    SDL_free(data);
+    window->internal = NULL;
+}
+
+void STANIX_SetWindowTitle(SDL_VideoDevice *_this, SDL_Window *window)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    twm_set_window_title(data->window, window->title);
+}
+
+bool STANIX_SetWindowPosition(SDL_VideoDevice *_this, SDL_Window *window)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    return twm_window_move(data->window, window->x, window->y) >= 0;
+}
+
+void STANIX_SetWindowSize(SDL_VideoDevice *_this, SDL_Window *window)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    twm_window_resize(data->window, window->w, window->h);
+}
+
+void STANIX_SetWindowBordered(SDL_VideoDevice *_this, SDL_Window *window, bool bordered)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    twm_set_window_attr(data->window, bordered ? TWM_ADD_ATTR : TWM_REMOVE_ATTR, TWM_ATTR_DECORED);
+}
+
+void STANIX_SetWindowResizable(SDL_VideoDevice *_this, SDL_Window *window, bool resizable)
+{
+    SDL_WindowData *data = (struct SDL_WindowData *)window->internal;
+    twm_set_window_attr(data->window, resizable ? TWM_ADD_ATTR : TWM_REMOVE_ATTR, TWM_ATTR_RESIZABLE);
+}
+
+#endif // SDL_VIDEO_DRIVER_STANIX
diff --git a/src/video/stanix/SDL_stanixwindow.h b/src/video/stanix/SDL_stanixwindow.h
new file mode 100644
index 0000000..48670b9
--- /dev/null
+++ b/src/video/stanix/SDL_stanixwindow.h
@@ -0,0 +1,44 @@
+/*
+  Simple DirectMedia Layer
+  Copyright (C) 1997-2026 Sam Lantinga <slouken@libsdl.org>
+
+  This software is provided 'as-is', without any express or implied
+  warranty.  In no event will the authors be held liable for any damages
+  arising from the use of this software.
+
+  Permission is granted to anyone to use this software for any purpose,
+  including commercial applications, and to alter it and redistribute it
+  freely, subject to the following restrictions:
+
+  1. The origin of this software must not be misrepresented; you must not
+     claim that you wrote the original software. If you use this software
+     in a product, an acknowledgment in the product documentation would be
+     appreciated but is not required.
+  2. Altered source versions must be plainly marked as such, and must not be
+     misrepresented as being the original software.
+  3. This notice may not be removed or altered from any source distribution.
+*/
+#include "SDL_internal.h"
+
+#ifndef SDL_stanixwindow_h_
+#define SDL_stanixwindow_h_
+
+#include <twm.h>
+
+struct SDL_WindowData
+{
+    twm_window_t window;
+    twm_fb_info_t fb_info;
+    void *framebuffer;
+    void *backbuffer;
+};
+
+extern bool STANIX_CreateWindow(SDL_VideoDevice *_this, SDL_Window *window, SDL_PropertiesID create_props);
+extern void STANIX_DestroyWindow(SDL_VideoDevice *_this, SDL_Window *window);
+extern void STANIX_SetWindowTitle(SDL_VideoDevice *_this, SDL_Window *window);
+extern bool STANIX_SetWindowPosition(SDL_VideoDevice *_this, SDL_Window *window);
+extern void STANIX_SetWindowSize(SDL_VideoDevice *_this, SDL_Window *window);
+extern void STANIX_SetWindowBordered(SDL_VideoDevice *_this, SDL_Window *window, bool bordered);
+extern void STANIX_SetWindowResizable(SDL_VideoDevice *_this, SDL_Window *window, bool resizable);
+
+#endif // SDL_x11window_h_
